{% extends 'partials/base.html.twig' %}

{% block content %}
<div class="page row">
<!--   <header id="header" class="row header-def">
  	<div class="inner-header">
		  <div class="title page-title">
	      <a href="{{ base_url == '' ? '/' : base_url }}"><img src="{{ theme_url }}/images/title.png" alt="title"></a>
	    </div>
      <nav class="page-navigation">
        x {% include 'partials/menu.html.twig' %}
      </nav>
	    <div class="description">
        <p>{{ page.content }}</p>
      </div>
	   </div>
  </header>-->
  {% include 'partials/header.html.twig' %}
  <div id="dialog"></div>
    <nav class="definition-list">
       {{alphabetical}}
<!--       <ul>
      {% for page in pages.children.visible %}
  			{% for child in page.children %}
  			<li class="definition large-2 columns">
      		<a href="{{child.url}}">
      			<h3>{{child.title}}</h3>
      			<p>{{child.content|truncate(100)}}</p>
      		</a>
      	</li>
  			{% endfor %}
  		{% endfor %}
  		</ul> -->
    </nav> 
</div> 
{% endblock %}